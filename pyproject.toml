[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "optizip"
version = "0.1.0"
description = "Optical context compression for Qwen"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["models*", "learning*", "inference*", "data*"]
